\begin{algorithm}[H]
\SetAlgoLined
    \If{level == 0} {
        \Return{playout(root, \textit{policy}, nb playout)}\;
    }
    \Else {
        \textit{bestScore} \(\leftarrow - \infty\)\;
        \For {N iterations} {
            (result, new) \(\leftarrow\) NRPA(\textit{level} \(- 1\), \textit{policy})\;
            \If {result \(\geq\) bestScore} {
                bestScore \(\leftarrow\) result\;
                seq \(\leftarrow\) new
            }
            policy \(\leftarrow\) Adapt(policy, seq)
        }
        \Return{(bestScore, seq)}
    }
 \caption{The SNRPA algorithm}
\end{algorithm}

